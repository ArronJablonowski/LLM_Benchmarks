# Application security review

Review `app.py`. Write `submission.json` with a `findings` array containing `line`, `cwe`, `title`, `precondition`, and `remediation`. Identify all three deliberately vulnerable constructs. Do not change `app.py`.
