# Risk prioritization

Write `submission.json` with `priority_order` (risk IDs), `csf_functions` (object mapping each risk ID to a NIST CSF 2.0 function), `owners`, and `rationale`. Rank by exposure and impact, not raw age.
