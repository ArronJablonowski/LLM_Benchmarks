# SOC alert triage

Write `submission.json` with `incident_alert`, `benign_alerts`, `severity`, `containment`, and `evidence`. Select the alert backed by correlated endpoint and identity evidence. Preserve the host for forensics.
