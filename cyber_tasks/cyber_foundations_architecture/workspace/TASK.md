# Segmented architecture review

Write `submission.json` with arrays named `critical_findings`, `priority_controls`, and `trust_boundaries`. Use the stable snake-case identifiers implied by the evidence. Include exactly the three highest-risk items in each array and a `rationale` string. Do not test any address.
