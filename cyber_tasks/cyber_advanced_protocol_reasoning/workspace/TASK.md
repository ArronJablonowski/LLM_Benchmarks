# Protocol failure diagnosis

Write `submission.json` with `root_causes` and `controls` arrays plus `reasoning`. Identify all three independent failures from the observations. Use concise snake-case labels.
