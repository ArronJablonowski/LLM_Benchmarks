# Mixed-confidence threat report

Write `submission.json` with arrays `confirmed_facts`, `assessments`, `indicators`, and `intelligence_gaps`, plus `attribution`. Use record IDs. Keep claims with `confidence: low` out of confirmed facts and do not name an actor.
