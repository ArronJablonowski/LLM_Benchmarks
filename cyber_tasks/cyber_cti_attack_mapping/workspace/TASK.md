# ATT&CK behavior mapping

Write `submission.json` with a `mappings` array. Each entry must contain `event_id`, the most specific `technique_id`, and `confidence`. Include only behaviors directly evidenced. Add an `attribution` field set to `unknown`.
