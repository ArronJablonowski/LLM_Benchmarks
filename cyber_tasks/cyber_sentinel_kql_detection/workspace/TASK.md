# Microsoft Sentinel KQL analytic

Create `query.kql` and `submission.json`. From `SigninLogs`, find 5 or more failures for an account/IP followed by a success within 10 minutes. Project account, IP, failure count, first failure, and success time.
