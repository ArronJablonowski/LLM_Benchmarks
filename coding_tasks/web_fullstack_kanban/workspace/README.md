# WebBoard

Build the full-stack Kanban application in [SPEC.md](SPEC.md). The finished
project must run offline with `python -m webboard --database board.db` and have
no runtime dependency outside the Python and browser standard libraries.

Run backend tests with `python -m unittest discover -s tests -v` and frontend
tests with `node --test tests/*.test.mjs`.
